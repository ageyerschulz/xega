
Execution model "MultiCore". 

Fork processes on a single machine.

Options tested:

excutionModel="MultiCore",
cores:  1-32.

Start on Notebook:

./GPXORmc.sh

Result: 

GPXORmc.out
GPXORmcNB.rds (after manual renaming)

Start in HPC-Cluster with Slurm:

sbatch GPXORmcSlurm.sh

Result:

GPXORmultiCore.output
GPXORmcSlurm.rds (after manual renaming)

