
library(xega) 

solution<-readRDS(file="lin105TSPstart.rds" ) 

AMDsgpermKid1mc<-xegaRun(solution$GAenv$penv,
    grammar=solution$GAenv$grammar,
    max=FALSE,
    algorithm="sgperm",
    popsize=200,
    generations=100,
    crossrate=0.2,
    mutrate=0.5,
    elitist=TRUE,
    #replay=42,
    maxdepth=7,
    maxtrials=5,
    codons=25,
    codonBits=0,
    codonPrecision="LCM",
    maxPBias=0.01,
    evalmethod="Deterministic",
    evalrep=1,
    reportEvalErrors=TRUE,
    genemap="Identity",
    decoder="DecodeGene",
    crossrate2=0.3,
    ivcrossrate="Const",
    crossover="CrossGene",
    uCrossSwap=0.2,
    mincrossdepth=1,
    maxcrossdepth=7,
    ivmutrate="Const",
    mutrate2=1,
    bitmutrate=0.005,
    bitmutrate2=0.01,
    maxmutdepth=3,
    minmutinsertiondepth=1,
    maxmutinsertiondepth=7,
    lambda=0.05,
    max2opt=20,
    scalefactor1=0.9,
    scalefactor2=0.3,
    scalefactor="Const",
    cutoffFit=0.5,
    mutation="MutateGeneMix",
    replication="Kid1PipelineG",
    initgene="InitGene",
    offset=1,
    eps=0.01,
    tournamentSize=2,
    selectionBias=1.5,
    maxTSR=1.5,
    selection="SUS",
    mateselection="SUS",
    selectionContinuation=TRUE,
    scaling="NoScaling",
    scalingThreshold=0,
    scalingExp=1,
    scalingExp2=1,
    rdmWeight=1,
    drMax=2,
    drMin=0.5,
    dispersionMeasure="var",
    scalingDelay=1,
    accept="All",
    alpha=0.99,
    beta=2,
    cooling="ExponentialMultiplicative",
    coolingPower=1,
    temp0=40,
    tempN=0.01,
    verbose=2,
    logevals=FALSE,
    allsolutions=FALSE,
    early=FALSE,
    terminationCondition="LEQ",
    terminationEps=0.01,
    terminationThreshold=2579,
    worstFitness=0,
    PACdelta=0.01,
    fSpace="Hilbert",
    cores=2,
    pipeline="PipeG",
    executionModel="MultiCore",
    uParApply=NULL,
    Cluster=NULL,
    profile=TRUE,
    batch=TRUE,
    anytime=FALSE,
    # path=path,
    migrate=TRUE,
    migrateEvery=1,
    pid=mpi.comm.rank(),
    npid=mpi.comm.size(),
    RmpiFNS=RmpiFNS,
    Send="mpi",
    Receive="mpi",
    semantics="byValue")

# saveRDS(AMDsgpermKid1mc, "AMDsgpermKid1mcSol.rds")

